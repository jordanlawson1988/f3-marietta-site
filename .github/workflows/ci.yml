name: CI
on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

jobs:
  lint-build:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - run: npm ci
      - run: npm run lint
      - run: npm run test:unit
      - run: npm run build
    env:
      # Neon dev/branch DB for DB-dependent specs (beatdown-builder etc.);
      # those specs stay red until this repo secret is set.
      DATABASE_URL: ${{ secrets.DATABASE_URL }}
      OPENAI_API_KEY: "sk-placeholder"
      SLACK_BOT_TOKEN: "xoxb-placeholder"
      SLACK_SIGNING_SECRET: "placeholder"

  e2e:
    needs: lint-build
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - run: npm ci
      - run: npx playwright install --with-deps chromium
      - run: npx playwright test --project=chromium
    env:
      # Neon dev/branch DB for DB-dependent specs (beatdown-builder etc.);
      # those specs stay red until this repo secret is set.
      DATABASE_URL: ${{ secrets.DATABASE_URL }}
      OPENAI_API_KEY: "sk-placeholder"
      SLACK_BOT_TOKEN: "xoxb-placeholder"
      SLACK_SIGNING_SECRET: "placeholder"
